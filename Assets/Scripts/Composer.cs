using UnityEngine;

public class Composer : MonoBehaviour
{

    [SerializeField] private GameObject baseElement;
    [SerializeField] private GameObject bodyElement;

    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
